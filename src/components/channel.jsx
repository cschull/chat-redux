import React from 'react';

const Channel = (props) => {
  return {
    <li className="channel-title" style={{ listStyleType: "none" }}>
      {this.props.channel}
    </li>
  }
};

export default Channel;
